# Скопируйте в файл .env в этой же папке и подставьте свои данные.
# Сервер читает .env при старте (не нужно каждый раз писать set в cmd).

DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/dance_school
DB_SCHEMA=public

# Опционально:
# STRIPE_DISABLED=1
# MOBILE_SYNC_TOKEN=dev-sync-token
